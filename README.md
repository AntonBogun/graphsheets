`npm install`

`npm run dev`

Open the link `http://localhost:****/` that vite shows

### Compiling text:
`npm install -g msdf-bmfont-xml`
cmd:
`msdf-bmfont -f json -m 512,512 -d 8 --pot --smart-size fonts/Inconsolata-Regular.ttf`