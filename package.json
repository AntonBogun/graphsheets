{
    "name": "webgl-typescript-project",
    "version": "1.0.0",
    "description": "WebGL TypeScript project",
    "main": "src/main.ts",
    "scripts": {
      "build": "python -m http.server | tsc --project tsconfig.json --watch",
      "dev": "tsc --project tsconfig.json --watch",
      "serve": "python -m http.server"
    },
    "devDependencies": {
      "typescript": "^5.8.3",
      "@types/webgl2": "^0.0.11"
    }
  }